import { Github, Linkedin, Mail } from "lucide-react";

const Nav = () => {
  return (
      <nav className="sticky top-0 z-50 bg-background flex items-center justify-between px-6 md:px-16 py-16">
      <h1 className="font-serif text-4xl md:text-5xl tracking-tight">
        Jorge Garcia
      </h1>
    </nav>
  );
};

export default Nav;
